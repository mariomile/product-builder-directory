---
phase: 01-stabilize
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - STAB-03
  - QUAL-02

must_haves:
  truths:
    - "Running npm install produces zero peer dependency warnings"
    - "No 'latest' or '^' prefix version specifiers remain in package.json"
    - "The radix-ui meta-package is absent from package.json and node_modules"
    - "npm run build exits cleanly"
  artifacts:
    - path: "package.json"
      provides: "Exact-pinned dependency manifest"
      contains: "no 'latest', no '^' prefix, no 'radix-ui' key"
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "no peer dep warnings on install"
---

<objective>
Pin all direct dependencies to exact versions and eliminate the radix-ui meta-package conflict.

Purpose: Prevent version drift on fresh installs and resolve the dual-install of Radix primitives that causes peer dependency warnings. Once done, `npm ci` on a clean checkout will always produce identical node_modules.
Output: Updated package.json with exact version strings throughout; radix-ui meta-package removed; next-themes removed.
</objective>

<execution_context>
@/Users/mariomiletta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomiletta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilize/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin all dependencies to exact versions</name>
  <files>package.json</files>
  <action>
Read package-lock.json to extract the exact resolved version for every direct dependency listed in package.json (both dependencies and devDependencies). The lock file's top-level `"packages"` object contains each package with a `"version"` field — use those values.

Rewrite package.json with these exact pins (no "latest", no "^", no "~"):

Dependencies (confirmed from lock file — verify each against package-lock.json before writing):
- @radix-ui/react-checkbox: 1.3.1
- @radix-ui/react-dropdown-menu: 2.1.14
- @radix-ui/react-label: 2.1.6
- @radix-ui/react-slot: 1.2.2
- @supabase/ssr: 0.9.0
- @supabase/supabase-js: 2.99.1
- class-variance-authority: (read from lock)
- clsx: (read from lock)
- lucide-react: (read from lock)
- next: 16.1.6
- react: (read from lock)
- react-dom: (read from lock)
- tailwind-merge: (read from lock)

DevDependencies:
- @eslint/eslintrc: (read from lock)
- @types/node: (read from lock)
- @types/react: (read from lock)
- @types/react-dom: (read from lock)
- autoprefixer: (read from lock)
- eslint: (read from lock)
- eslint-config-next: 16.1.6  (MUST match next version exactly — current value of 15.3.1 is misaligned)
- postcss: (read from lock)
- tailwindcss: (read from lock)
- tailwindcss-animate: (read from lock)
- typescript: (read from lock)

Do NOT include radix-ui or next-themes in the rewritten file (both are removed in the next task). Do NOT include them even with a pinned version.

Keep the scripts block unchanged. Preserve key order: private, scripts, dependencies, devDependencies.
  </action>
  <verify>
    <automated>grep -E '"latest"|"\^|"~' /Users/mariomiletta/conductor/workspaces/builder-directory/douala/package.json | wc -l</automated>
  </verify>
  <done>Output of the grep command is 0 — no floating specifiers remain in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Remove radix-ui meta-package and next-themes, reinstall, verify clean</name>
  <files>package.json</files>
  <action>
Confirm package.json (written in Task 1) does NOT contain "radix-ui" (the meta-package) or "next-themes". These must already be absent from the file written in Task 1 — this task verifies and then reinstalls.

Run the following in order:
1. `npm install` — reinstalls from the freshly pinned package.json. This will also remove radix-ui and next-themes from node_modules since they are no longer listed.
2. Verify no peer dependency warnings: `npm install 2>&1 | grep -iE "warn|peer dep"` — must return empty output.
3. Verify build: `npm run build` — must exit 0.

If npm install emits peer dep warnings, inspect which package is causing them with `npm ls <package-name>` and resolve by adjusting the pinned version in package.json to match what npm resolves. Then re-run steps 1-3.

Do NOT use `npm uninstall` — the packages are already absent from package.json written in Task 1. Just run `npm install` to sync node_modules.
  </action>
  <verify>
    <automated>cd /Users/mariomiletta/conductor/workspaces/builder-directory/douala && npm install 2>&1 | grep -iE "warn|peer dep" | wc -l && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
- `npm install` peer warning grep returns 0 lines
- `npm run build` exits 0 and shows "Compiled successfully" or equivalent
- `grep '"radix-ui"' package.json` returns empty
- `grep '"next-themes"' package.json` returns empty
  </done>
</task>

</tasks>

<verification>
After both tasks complete, confirm all three success signals:

1. `grep -E '"latest"|"\^|"~' package.json | wc -l` → 0
2. `npm install 2>&1 | grep -iE "warn|peer dep"` → empty
3. `npm run build` → exits 0
4. `grep '"radix-ui"' package.json` → no output
</verification>

<success_criteria>
- package.json contains only bare version strings (no "latest", "^", "~")
- radix-ui and next-themes are absent from package.json and node_modules
- eslint-config-next version matches next version (both 16.1.6)
- `npm install` produces zero peer dependency warnings
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-01-SUMMARY.md` documenting:
- Final exact versions written into package.json
- Confirmation that radix-ui and next-themes were removed
- npm install and build verification results
</output>
