---
phase: 04-infrastructure-and-deploy
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - ".vercel/project.json"
autonomous: false
requirements:
  - INFR-04

user_setup:
  - service: vercel
    why: "vercel link is interactive — requires user to select org/scope and confirm project settings"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: ".env.local — value: https://miclgfzbdzjhdurdeqmt.supabase.co"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: ".env.local — value: sb_publishable_DkR-o-xH96lnFBTqIy-rRw_kftRRaWr"
      - name: SYNC_SECRET
        source: "Value generated in Plan 02 (edge function secrets step) — must match Supabase SYNC_SECRET"
    dashboard_config:
      - task: "Confirm GitHub repo connected during vercel link"
        location: "Vercel → Import Git Repository → github.com/mariomile/product-builder-directory"

must_haves:
  truths:
    - "A production Vercel URL loads the homepage with resources visible"
    - "Search, filter by type/pillar, and resource detail page all work on the production URL"
    - "Vercel env vars list shows all 3 required vars for production environment"
    - "instrumentation.ts startup validation passes (no crash on cold start)"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel project link metadata created by vercel link"
  key_links:
    - from: "github.com/mariomile/product-builder-directory (main branch)"
      to: "Vercel project"
      via: "vercel link + GitHub integration"
      pattern: "vercel link"
    - from: "NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY"
      to: "Vercel production environment"
      via: "vercel env add"
      pattern: "vercel env add"
    - from: "instrumentation.ts"
      to: "NEXT_PUBLIC_SUPABASE_URL"
      via: "startup env validation"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE_URL"
---

<objective>
Create the Vercel project, link it to the GitHub repository, set all required environment variables, trigger a production deploy, and verify the production URL passes a full smoke test.

Purpose: INFR-04 requires the application to be live on a production URL with correct env vars and a passing smoke test. This is the final plan in the phase — the public URL goes live only after the DB migration (Plan 01) has closed the RLS security gap.

Output: Production Vercel URL serving the application from main branch with live Supabase data.
</objective>

<execution_context>
@/Users/mariomiletta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomiletta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-infrastructure-and-deploy/04-CONTEXT.md
@.planning/phases/04-infrastructure-and-deploy/04-RESEARCH.md
@.planning/phases/04-infrastructure-and-deploy/04-01-SUMMARY.md

<interfaces>
<!-- Vercel CLI location: ~/.local/bin/vercel (v50.4.11) -->
<!-- Git remote: github.com/mariomile/product-builder-directory -->
<!-- Branch for production: main -->

Required env vars for Vercel production:
  NEXT_PUBLIC_SUPABASE_URL=https://miclgfzbdzjhdurdeqmt.supabase.co
  NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_DkR-o-xH96lnFBTqIy-rRw_kftRRaWr
  SYNC_SECRET=<value from Plan 02 edge function secrets step>

instrumentation.ts validates these at startup — ALL must be set before first deploy or app crashes.

Vercel CLI sequence:
  vercel whoami                              # confirm authenticated
  vercel link                                # interactive: creates project, connects GitHub
  vercel env add NEXT_PUBLIC_SUPABASE_URL production
  vercel env add NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY production
  vercel env add SYNC_SECRET production
  vercel env ls production                   # verify all 3 present
  vercel --prod                              # deploy from main branch

Smoke test checklist (5 steps):
  1. Homepage loads with 20 resources visible
  2. Type a search query — results filter
  3. Click a type/pillar filter — results update
  4. Pagination shows correctly (20 items/page, PaginationBar visible if >20)
  5. Click a resource card — detail page loads
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vercel project, set env vars, and deploy to production</name>
  <files>.vercel/project.json</files>
  <action>
    Run from the project root (`/Users/mariomiletta/conductor/workspaces/builder-directory/douala`):

    Step 1 — Verify Vercel CLI is authenticated:
    ```bash
    ~/.local/bin/vercel whoami
    ```
    If not authenticated, run `~/.local/bin/vercel login` first and complete browser auth.

    Step 2 — Link to Vercel project:
    ```bash
    ~/.local/bin/vercel link
    ```
    When prompted:
    - Set up and deploy: Yes
    - Scope: select the correct Vercel account
    - Link to existing project: No (create new)
    - Project name: accept default or use `product-builder-directory`
    - Directory: . (project root)
    - GitHub repo connection: connect to `github.com/mariomile/product-builder-directory`, branch `main`

    Step 3 — Add environment variables for production:
    ```bash
    ~/.local/bin/vercel env add NEXT_PUBLIC_SUPABASE_URL production
    # When prompted: https://miclgfzbdzjhdurdeqmt.supabase.co

    ~/.local/bin/vercel env add NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY production
    # When prompted: sb_publishable_DkR-o-xH96lnFBTqIy-rRw_kftRRaWr

    ~/.local/bin/vercel env add SYNC_SECRET production
    # When prompted: <SYNC_SECRET value from Plan 02>
    ```

    Step 4 — Verify all env vars are set:
    ```bash
    ~/.local/bin/vercel env ls production
    ```
    Must show all 3 vars before proceeding.

    Step 5 — Deploy to production:
    ```bash
    ~/.local/bin/vercel --prod
    ```
    Wait for deploy to complete. Note the production URL (format: `https://product-builder-directory-xyz.vercel.app` or similar).

    CRITICAL: instrumentation.ts validates NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY at startup. If either is missing, the deployed app will crash on cold start. Confirm `vercel env ls` shows both before deploying.
  </action>
  <verify>
    <automated>~/.local/bin/vercel env ls production — all 3 vars present; vercel --prod exits 0 and prints production URL</automated>
  </verify>
  <done>Vercel project created, linked to GitHub, all 3 env vars set for production, deploy completed with a production URL returned</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Smoke test the production URL end-to-end</name>
  <files></files>
  <action>Human verification required — open the production URL and run the 5-step smoke test below</action>
  <what-built>Production Vercel URL is live. Smoke test to confirm all functionality works end-to-end.</what-built>
  <how-to-verify>
    Open the production URL from the vercel --prod output. Run the 5-step smoke test:

    1. Homepage loads — confirm 20 resources are visible in the terminal grid layout
    2. Search — type "supabase" (or any known resource name) in the search bar; results should filter after 300ms debounce
    3. Filter — click a type badge or pillar filter; resource list should update, page param should reset
    4. Pagination — if more than 20 resources exist (post-sync), PaginationBar should appear; navigate to page 2
    5. Detail page — click any resource card; detail page should load with full resource info

    Also verify:
    - No crash/error boundary visible on homepage load (instrumentation.ts env validation passed)
    - Terminal aesthetic is intact (Geist Mono, black/white/cyan, no rounded corners)
    - URL is HTTPS
  </how-to-verify>
  <verify>
    <automated>MISSING — manual browser verification: open production URL and complete 5-step smoke test</automated>
  </verify>
  <done>All 5 smoke test steps pass; production URL is live with correct terminal aesthetic and no errors</done>
  <resume-signal>Type "smoke test passed" with the production URL, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Production URL must pass all 5 smoke test steps:
1. Homepage: 20 resources visible
2. Search: filters results with 300ms debounce
3. Type/pillar filter: updates results, resets page
4. Pagination: shows when >20 resources; navigates correctly
5. Detail page: loads from card click

Plus:
- `vercel env ls production` shows NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SYNC_SECRET
- No error boundary visible on first load
</verification>

<success_criteria>
- Vercel project created and linked to github.com/mariomile/product-builder-directory (main branch)
- All 3 required env vars set for production environment
- `vercel --prod` deploy completed without errors
- Production URL is live and returns HTTP 200
- All 5 smoke test steps pass
- Terminal aesthetic intact on production URL
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure-and-deploy/04-03-SUMMARY.md`
Include the production URL prominently — this is the milestone deliverable.
</output>
