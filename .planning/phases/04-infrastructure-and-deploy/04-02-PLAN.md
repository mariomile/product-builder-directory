---
phase: 04-infrastructure-and-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - INFR-02
  - INFR-03

user_setup:
  - service: supabase-edge-functions
    why: "Edge function secrets cannot be set via CLI — must be configured in Supabase Dashboard"
    env_vars:
      - name: RAINDROP_TOKEN
        source: "Raindrop.io → App settings → Integrations → Create Test Token"
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → API Keys"
      - name: SUPABASE_URL
        source: "Value: https://miclgfzbdzjhdurdeqmt.supabase.co (from .env.local)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role key"
      - name: SYNC_SECRET
        source: "Generate with: openssl rand -hex 32 — use same value in Vercel (Plan 03)"
    dashboard_config:
      - task: "Set all 5 secrets in Edge Function secrets"
        location: "Supabase Dashboard → Edge Functions → sync-raindrop → Secrets"
      - task: "Configure cron schedule after edge function deploys"
        location: "Supabase Dashboard → Edge Functions → sync-raindrop → Schedule (set to: 0 */6 * * *)"

must_haves:
  truths:
    - "sync-raindrop edge function is deployed and reachable at its Supabase URL"
    - "A manual POST to the edge function with Authorization header returns 200 (or processes sync)"
    - "5 MCP verification queries via execute_sql return correct results from the live database"
    - "Cron schedule is set to every 6 hours on the deployed edge function"
  artifacts:
    - path: "supabase/functions/sync-raindrop/index.ts"
      provides: "Edge function — already written, must be deployed"
      contains: "SYNC_SECRET"
  key_links:
    - from: "supabase/functions/sync-raindrop/index.ts"
      to: "live Supabase project (miclgfzbdzjhdurdeqmt)"
      via: "deploy_edge_function MCP tool"
      pattern: "deploy_edge_function"
    - from: "cron schedule"
      to: "sync-raindrop edge function"
      via: "Supabase Dashboard → Schedule (0 */6 * * *)"
      pattern: "0 \\*/6 \\* \\* \\*"
---

<objective>
Deploy the Raindrop sync edge function to the live Supabase project, configure cron schedule and secrets, and run 5 MCP verification queries to confirm the live database is queryable and correct.

Purpose: INFR-03 requires the sync pipeline to be operational with cron scheduling. INFR-02 requires confirming MCP can query the live database with correct results. This plan completes both after the DB is secured (Plan 01).

Output: sync-raindrop edge function live at Supabase, cron scheduled every 6 hours, 5 MCP queries verified against production data.
</objective>

<execution_context>
@/Users/mariomiletta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomiletta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-infrastructure-and-deploy/04-CONTEXT.md
@.planning/phases/04-infrastructure-and-deploy/04-RESEARCH.md
@.planning/phases/04-infrastructure-and-deploy/04-01-SUMMARY.md

<interfaces>
<!-- Supabase project: miclgfzbdzjhdurdeqmt.supabase.co -->
<!-- MCP tools: apply_migration, execute_sql, deploy_edge_function -->

Edge function location: supabase/functions/sync-raindrop/index.ts
Edge function requires these secrets (set in Supabase Dashboard, NOT in code):
  - SYNC_SECRET — authorization bearer token (must match Vercel SYNC_SECRET in Plan 03)
  - RAINDROP_TOKEN — Raindrop.io bearer token
  - ANTHROPIC_API_KEY — Anthropic API key
  - SUPABASE_URL — https://miclgfzbdzjhdurdeqmt.supabase.co
  - SUPABASE_SERVICE_ROLE_KEY — from Supabase Dashboard → Project Settings → API

Auth pattern in edge function (from src):
  Authorization check happens BEFORE try block.
  Missing SYNC_SECRET returns 401. Valid header continues to sync logic.
  Header format: Authorization: Bearer <SYNC_SECRET>

Cron expression: 0 */6 * * * (every 6 hours)

5 MCP verification queries:
  1. SELECT COUNT(*) FROM resources;
     Expected: 20

  2. SELECT name, type, pillar FROM resources WHERE is_featured = true ORDER BY name;
     Expected: featured resources list

  3. SELECT name, type FROM resources WHERE pillar = 'stack' ORDER BY name;
     Expected: stack-pillar resources

  4. SELECT name, type, pillar FROM resources
     WHERE name ILIKE '%supabase%' OR description ILIKE '%supabase%';
     Expected: Supabase resource(s)

  5. SELECT policyname, cmd FROM pg_policies WHERE tablename = 'resources' ORDER BY policyname;
     Expected: 4 policies (DELETE, INSERT, SELECT, UPDATE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy sync-raindrop edge function via Supabase MCP</name>
  <files>supabase/functions/sync-raindrop/index.ts</files>
  <action>
    Use the `deploy_edge_function` MCP tool to deploy `supabase/functions/sync-raindrop` to the live Supabase project (`miclgfzbdzjhdurdeqmt`).

    Function name: `sync-raindrop`
    Source: `supabase/functions/sync-raindrop/index.ts`

    After deploy completes, note the edge function URL (format: `https://miclgfzbdzjhdurdeqmt.supabase.co/functions/v1/sync-raindrop`).

    Do NOT invoke the function yet — secrets must be set first (checkpoint below).
  </action>
  <verify>
    <automated>deploy_edge_function MCP tool returns success status for sync-raindrop</automated>
  </verify>
  <done>sync-raindrop edge function is deployed to miclgfzbdzjhdurdeqmt.supabase.co; function URL is reachable</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Set edge function secrets and cron schedule in Supabase Dashboard</name>
  <files></files>
  <action>Human action required — see how-to-verify for exact steps</action>
  <what-built>sync-raindrop edge function is deployed. Secrets must be set in Supabase Dashboard before it can run.</what-built>
  <how-to-verify>
    1. Open: https://supabase.com/dashboard/project/miclgfzbdzjhdurdeqmt/functions
    2. Click "sync-raindrop" → "Secrets" tab
    3. Add ALL 5 secrets (if any already exist, update them):
       - SYNC_SECRET: generate with `openssl rand -hex 32` — SAVE THIS VALUE, you need it for Vercel (Plan 03) too
       - RAINDROP_TOKEN: from Raindrop.io → App settings → Integrations
       - ANTHROPIC_API_KEY: from Anthropic Console → API Keys
       - SUPABASE_URL: https://miclgfzbdzjhdurdeqmt.supabase.co
       - SUPABASE_SERVICE_ROLE_KEY: from Supabase Dashboard → Project Settings → API → service_role (secret)
    4. After saving all 5 secrets, set the cron schedule:
       - Still on sync-raindrop function page → "Schedule" tab
       - Set schedule: 0 */6 * * * (every 6 hours)
       - If the Dashboard cron UI does not support Authorization headers, use pg_cron via execute_sql (see Research doc)
    5. Test the function manually:
       - POST to https://miclgfzbdzjhdurdeqmt.supabase.co/functions/v1/sync-raindrop
       - Header: Authorization: Bearer <SYNC_SECRET value you just set>
       - Expected: 200 response (may take 10-30s as it calls Raindrop + Claude)
  </how-to-verify>
  <verify>
    <automated>MISSING — manual verification: all 5 secrets set in Supabase Dashboard + manual POST returns 200</automated>
  </verify>
  <done>All 5 secrets set, cron scheduled at 0 */6 * * *, manual POST to edge function returns 200</done>
  <resume-signal>Type "secrets set and function tested" once all 5 secrets are saved, cron is scheduled, and the manual POST returned 200</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run 5 MCP verification queries against live DB</name>
  <files></files>
  <action>
    Run all 5 verification queries via `execute_sql` and confirm results match expectations:

    Query 1 — Total count:
    ```sql
    SELECT COUNT(*) FROM resources;
    ```
    Expected: 20 (if sync added resources, count may be > 20 — that is acceptable)

    Query 2 — Featured resources:
    ```sql
    SELECT name, type, pillar FROM resources WHERE is_featured = true ORDER BY name;
    ```
    Expected: at least 1 featured resource returned

    Query 3 — Filter by pillar:
    ```sql
    SELECT name, type FROM resources WHERE pillar = 'stack' ORDER BY name;
    ```
    Expected: resources with pillar = 'stack' returned (at least 1)

    Query 4 — Full-text search:
    ```sql
    SELECT name, type, pillar FROM resources
    WHERE name ILIKE '%supabase%' OR description ILIKE '%supabase%';
    ```
    Expected: at least 1 Supabase-related resource returned

    Query 5 — RLS policy verification:
    ```sql
    SELECT policyname, cmd FROM pg_policies WHERE tablename = 'resources' ORDER BY policyname;
    ```
    Expected: 4 rows — Authenticated delete (DELETE), Authenticated insert (INSERT), Public read access (SELECT), Authenticated update (UPDATE)

    Record each query result in the task summary. If any query returns unexpected results, investigate and document.
  </action>
  <verify>
    <automated>execute_sql: SELECT COUNT(*) FROM resources — expect >= 20; SELECT COUNT(*) FROM pg_policies WHERE tablename='resources' — expect 4</automated>
  </verify>
  <done>All 5 MCP queries return correct results; live DB confirmed queryable with proper RLS policies and data</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Edge function URL responds to authenticated POST (200 status)
2. `SELECT COUNT(*) FROM resources` returns >= 20
3. `SELECT COUNT(*) FROM pg_policies WHERE tablename = 'resources'` returns 4
4. Cron schedule shows `0 */6 * * *` in Supabase Dashboard
5. All 5 MCP queries return non-empty results matching expected shapes
</verification>

<success_criteria>
- sync-raindrop edge function deployed and reachable
- All 5 edge function secrets set in Supabase Dashboard
- Cron scheduled at 0 */6 * * * (every 6 hours)
- Manual invocation with valid SYNC_SECRET returns 200
- 5 MCP verification queries all pass against live DB
- SYNC_SECRET value documented for use in Plan 03 (Vercel env vars)
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure-and-deploy/04-02-SUMMARY.md`
Include the edge function URL and note whether the SYNC_SECRET value is ready for Plan 03.
</output>
