---
phase: 02-terminal-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
  - tailwind.config.ts
  - lib/constants.ts
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - QUAL-01

must_haves:
  truths:
    - "The <html> element carries className='dark' on every page load"
    - "CSS variables in globals.css resolve to the terminal palette (black background, near-white foreground, cyan primary, zero radius)"
    - "Geist Mono is the active font-family on body — DevTools shows --font-geist-mono or Geist Mono"
    - "lib/constants.ts exports TYPE_LABELS, PILLAR_LABELS, TYPE_BADGE_CLASSES, TYPES, PILLARS, LEVELS with zero imports"
  artifacts:
    - path: "lib/constants.ts"
      provides: "All shared label/color/filter maps"
      exports: ["TYPE_LABELS", "PILLAR_LABELS", "TYPE_BADGE_CLASSES", "TYPES", "PILLARS", "LEVELS"]
    - path: "app/layout.tsx"
      provides: "Root layout with dark class and Geist Mono"
      contains: "className=\"dark\""
    - path: "app/globals.css"
      provides: "Terminal CSS variable palette"
      contains: "--primary: 180 100% 50%"
    - path: "tailwind.config.ts"
      provides: "Font family override wiring Geist Mono to font-sans and font-mono"
      contains: "var(--font-geist-mono)"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "dark class activates .dark CSS variable block"
      pattern: "className=\"dark\""
    - from: "tailwind.config.ts"
      to: "app/layout.tsx"
      via: "font-sans resolves to --font-geist-mono variable set on <body>"
      pattern: "var\\(--font-geist-mono\\)"
---

<objective>
Establish the terminal design system foundation: forced dark mode, the black/white/cyan CSS variable palette, Geist Mono font globally, and a single constants file that eliminates duplicated label maps from three component files.

Purpose: Every downstream component restyle (Plan 02-02) depends on these CSS variables and the constants file being in place first. Getting the foundation wrong invalidates all subsequent work.
Output: Four files modified/created — layout.tsx, globals.css, tailwind.config.ts, lib/constants.ts.
</objective>

<execution_context>
@/Users/mariomiletta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomiletta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-terminal-design-system/02-RESEARCH.md

<interfaces>
<!-- Key existing code the executor needs. No codebase exploration required. -->

From app/layout.tsx (current — to be replaced):
```tsx
import { Geist } from "next/font/google";
const geistSans = Geist({ variable: "--font-geist-sans", display: "swap", subsets: ["latin"] });
<html lang="en">
  <body className={`${geistSans.className} antialiased`}>
```

From app/globals.css (current — to be replaced):
```css
:root { --radius: 0.5rem; --primary: 0 0% 9%; /* light theme values */ }
.dark { --background: 0 0% 3.9%; /* shadcn default dark */ }
```

From tailwind.config.ts (current — to be extended):
```ts
darkMode: ["class"],
theme: { extend: { borderRadius: { lg: "var(--radius)", md: "calc(var(--radius) - 2px)", sm: "calc(var(--radius) - 4px)" } } }
```

From components/resource-card.tsx (data to migrate to constants):
```ts
const typeLabels = { tool: "Tool", course: "Course", article: "Article", newsletter: "Newsletter", book: "Book", podcast: "Podcast", video: "Video", community: "Community", x_post: "X Post", framework: "Framework" };
const pillarLabels = { discovery: "Discovery", design: "Design", delivery: "Delivery", strategy: "Strategy", stack: "Stack & Tools", meta_skill: "Meta-skill" };
const typeColors = { tool: "bg-blue-100 text-blue-800 ...", /* rainbow */ };
```

From components/filters.tsx (data to migrate to constants):
```ts
const TYPES = [{ value: "tool", label: "Tool" }, ...];
const PILLARS = [{ value: "discovery", label: "Discovery" }, ...];
const LEVELS = [{ value: "beginner", label: "Beginner" }, { value: "intermediate", label: "Intermediate" }, { value: "advanced", label: "Advanced" }];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force dark mode, swap to Geist Mono, rewrite terminal CSS palette</name>
  <files>app/layout.tsx, app/globals.css, tailwind.config.ts</files>
  <action>
**app/layout.tsx** — replace the entire file:
- Import `Geist_Mono` (not `Geist`) from `next/font/google`
- Configure: `variable: "--font-geist-mono"`, `display: "swap"`, `subsets: ["latin"]`
- Add `className="dark"` to `<html>` element (CRITICAL: on html, not body)
- Apply `${geistMono.variable} font-mono antialiased` to `<body>` (use `.variable` not `.className` so Tailwind's font-mono utility resolves it)
- Keep existing metadata export and defaultUrl logic unchanged

**tailwind.config.ts** — add fontFamily to theme.extend (keep all existing entries):
```ts
fontFamily: {
  sans: ["var(--font-geist-mono)", "Courier New", "monospace"],
  mono: ["var(--font-geist-mono)", "Courier New", "monospace"],
},
```
This ensures shadcn's `font-sans` classes resolve to Geist Mono automatically.

**app/globals.css** — replace both `:root` and `.dark` blocks with terminal palette. Keep the `@tailwind base/components/utilities` directives and the `@layer base { * { @apply border-border } body { @apply bg-background text-foreground } }` block unchanged. Replace ONLY the CSS variable blocks:

```css
@layer base {
  :root {
    --background: 0 0% 0%;
    --foreground: 0 0% 98%;
    --card: 0 0% 4%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 4%;
    --popover-foreground: 0 0% 98%;
    --primary: 180 100% 50%;
    --primary-foreground: 0 0% 0%;
    --secondary: 0 0% 10%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 10%;
    --muted-foreground: 0 0% 60%;
    --accent: 0 0% 15%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 20%;
    --input: 0 0% 15%;
    --ring: 180 100% 50%;
    --radius: 0rem;
  }
  .dark {
    --background: 0 0% 0%;
    --foreground: 0 0% 98%;
    --card: 0 0% 4%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 4%;
    --popover-foreground: 0 0% 98%;
    --primary: 180 100% 50%;
    --primary-foreground: 0 0% 0%;
    --secondary: 0 0% 10%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 10%;
    --muted-foreground: 0 0% 60%;
    --accent: 0 0% 15%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 84% 60%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 20%;
    --input: 0 0% 15%;
    --ring: 180 100% 50%;
    --radius: 0rem;
  }
}
```

PITFALLS TO AVOID:
- Do NOT write `--primary: hsl(180, 100%, 50%)` — write `--primary: 180 100% 50%` (no hsl wrapper)
- Do NOT put dark class on `<body>` — it must be on `<html>`
- Do NOT use `suppressHydrationWarning` — static class never causes mismatch
- Do NOT remove the chart-* variables if they exist — just leave them or omit (not used in this app)
  </action>
  <verify>
    <automated>cd /Users/mariomiletta/conductor/workspaces/builder-directory/douala && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. layout.tsx has `className="dark"` on html. globals.css has `--primary: 180 100% 50%` and `--radius: 0rem`. tailwind.config.ts has fontFamily with `var(--font-geist-mono)`.</done>
</task>

<task type="auto">
  <name>Task 2: Create lib/constants.ts — single source of truth for all label and filter maps</name>
  <files>lib/constants.ts</files>
  <action>
Create `lib/constants.ts` as a pure data file with ZERO imports. All values are plain objects and arrays — no imported types, no Next.js imports, no utils imports (would cause circular dependency risk).

Export the following:

```ts
// lib/constants.ts
// Pure data — no imports. Must remain import-free to avoid circular dependency
// and to stay edge-function compatible (Phase 4).

export const TYPE_LABELS: Record<string, string> = {
  tool: "Tool",
  course: "Course",
  article: "Article",
  newsletter: "Newsletter",
  book: "Book",
  podcast: "Podcast",
  video: "Video",
  community: "Community",
  x_post: "X Post",
  framework: "Framework",
};

export const PILLAR_LABELS: Record<string, string> = {
  discovery: "Discovery",
  design: "Design",
  delivery: "Delivery",
  strategy: "Strategy",
  stack: "Stack & Tools",
  meta_skill: "Meta-skill",
};

// Monochrome terminal badge classes — replaces rainbow typeColors.
// All use outline variant with border/text from terminal palette.
// Primary type badge uses cyan (border-primary text-primary).
export const TYPE_BADGE_CLASSES: Record<string, string> = {
  tool: "border-primary text-primary",
  course: "border-border text-foreground",
  article: "border-border text-foreground",
  newsletter: "border-border text-foreground",
  book: "border-border text-foreground",
  podcast: "border-border text-foreground",
  video: "border-border text-foreground",
  community: "border-border text-foreground",
  x_post: "border-border text-muted-foreground",
  framework: "border-primary text-primary",
};

export const TYPES = [
  { value: "tool", label: "Tool" },
  { value: "course", label: "Course" },
  { value: "article", label: "Article" },
  { value: "newsletter", label: "Newsletter" },
  { value: "book", label: "Book" },
  { value: "podcast", label: "Podcast" },
  { value: "video", label: "Video" },
  { value: "community", label: "Community" },
  { value: "x_post", label: "X Post" },
  { value: "framework", label: "Framework" },
];

export const PILLARS = [
  { value: "discovery", label: "Discovery" },
  { value: "design", label: "Design" },
  { value: "delivery", label: "Delivery" },
  { value: "strategy", label: "Strategy" },
  { value: "stack", label: "Stack & Tools" },
  { value: "meta_skill", label: "Meta-skill" },
];

export const LEVELS = [
  { value: "beginner", label: "Beginner" },
  { value: "intermediate", label: "Intermediate" },
  { value: "advanced", label: "Advanced" },
];
```

PITFALLS TO AVOID:
- Do NOT add any import statements — zero imports required
- Do NOT import from lib/validators.ts, lib/queries.ts, or lib/utils.ts
  </action>
  <verify>
    <automated>cd /Users/mariomiletta/conductor/workspaces/builder-directory/douala && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>lib/constants.ts exists with all 6 exports. No import statements in the file. Build passes. grep for "const typeLabels" in components/ and app/ returns results only in resource-card.tsx and [slug]/page.tsx (those get removed in Plan 02-02).</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the foundation:

1. Build is green: `npm run build` exits 0
2. Dark mode: `grep -n 'className="dark"' app/layout.tsx` — should show the html element
3. Geist Mono: `grep -n 'Geist_Mono' app/layout.tsx` — should exist
4. Terminal palette: `grep -n 'primary: 180' app/globals.css` — should show `--primary: 180 100% 50%`
5. Zero radius: `grep -n 'radius: 0rem' app/globals.css` — should match
6. Constants exist: `grep -n 'export const TYPE_LABELS' lib/constants.ts` — should match
7. No imports in constants: `grep -n '^import' lib/constants.ts` — should return empty
8. Font wiring: `grep -n 'font-geist-mono' tailwind.config.ts` — should match
</verification>

<success_criteria>
- `npm run build` exits 0
- `<html>` has `className="dark"` in layout.tsx
- CSS variables use terminal palette: `--background: 0 0% 0%`, `--primary: 180 100% 50%`, `--radius: 0rem`
- `lib/constants.ts` exports TYPE_LABELS, PILLAR_LABELS, TYPE_BADGE_CLASSES, TYPES, PILLARS, LEVELS
- Tailwind config maps `font-sans` and `font-mono` to `var(--font-geist-mono)`
</success_criteria>

<output>
After completion, create `.planning/phases/02-terminal-design-system/02-01-SUMMARY.md` following the summary template at @/Users/mariomiletta/.claude/get-shit-done/templates/summary.md
</output>
