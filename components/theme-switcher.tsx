"use client";

// next-themes removed in stabilization phase.
// ThemeSwitcher is retained as a placeholder; dark mode will be
// re-evaluated in Phase 2 (Design Overhaul).
const ThemeSwitcher = () => {
  return null;
};

export { ThemeSwitcher };
